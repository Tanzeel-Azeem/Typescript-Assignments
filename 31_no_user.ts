let userNames: string[]= ['Admin', 'ali','khan','ayan','ibad'];
if(userNames.length === 0){
    console.log('We need to find some users!!!')
}else {
    userNames= [];

    console.log('All the users names have been removed ' + userNames.length)
}